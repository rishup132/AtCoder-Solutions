#include <bits/stdc++.h>

using namespace std;

int main()
{
    freopen("input.txt","w",stdout);

    int t = 1;
    cout << 1 << endl;

    while(t--)
    {
        long long temp;
        temp = (long long)rand();
        temp *= (long long)rand();
        cout << temp << endl;
    }
}